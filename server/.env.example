
# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration 
DB_HOST=your-production-db-host
DB_PORT=3306
DB_NAME=tecace_devices
DB_USER=your_production_db_user
DB_PASSWORD=your_strong_production_password

# Session Configuration
SESSION_SECRET=generate_a_strong_random_secret_key_for_production

# Client URL - Set to your production frontend URL
CLIENT_URL=https://your-production-frontend-domain.com

# Database Reset Configuration (IMPORTANT: set to false in production)
RESET_DATABASE=false

# Database logging options (set to false in production for performance)
DB_DEBUG=false
SQL_LOG=false
