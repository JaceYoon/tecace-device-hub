
import { RequestStore } from './store';

// Create and export the singleton instance
export const requestStore = new RequestStore();

// Re-export any types or utilities as needed
export * from './store';
